import React from 'react';
import { tw } from 'twind';

export const Header = () => {
  return(
    <section className={tw`w-full bg-red-500 gap h-32`}>
      <span>Header de prueba</span>
    </section>
  )
}
